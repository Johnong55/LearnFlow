// Keep production builds independent from Google Fonts. If Inter is installed
// locally it is used first; otherwise the Vietnamese-safe system stack applies.
export const fontClassNames = "font-inter-fallback";
